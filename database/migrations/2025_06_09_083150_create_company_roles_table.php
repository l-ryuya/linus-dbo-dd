<?php

declare(strict_types=1);

use Illuminate\Database\Migrations\Migration;
use Illuminate\Database\Schema\Blueprint;
use Illuminate\Support\Facades\Schema;

return new class extends Migration {
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create('company_roles', function (Blueprint $table) {
            $table->comment('法人が持ち得る役割を定義するマスタ');

            // システム識別子
            $table->unsignedBigInteger('company_role_id')
                ->primary()
                ->comment('内部用連番 PK');

            // ビジネス識別子
            $table->string('company_role_code')
                ->unique()
                ->comment('役割コード（システム内部参照用、ユニーク）');

            // 属性
            $table->string('remarks')->nullable()
                ->comment('役割に関する備考');

            // 監査系
            $table->timestamp('created_at')->useCurrent()
                ->comment('レコード作成日時');
            $table->timestamp('updated_at')->useCurrent()
                ->comment('レコード更新日時');
            $table->timestamp('deleted_at')->nullable()
                ->comment('レコード削除日時（論理削除）');
        });

        // ID列にIDENTITYを追加する
        DB::statement("ALTER TABLE company_roles ALTER COLUMN company_role_id ADD GENERATED BY DEFAULT AS IDENTITY");
        // パーシャルインデックス
        DB::statement("CREATE INDEX idx_company_roles_deleted_null ON company_roles (company_role_id) WHERE deleted_at IS NULL");
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::dropIfExists('company_roles');
    }
};
